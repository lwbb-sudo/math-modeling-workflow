\section{模型设计与方法}

% 写作提示：从问题需求出发说明总体流程、变量与参数、方法选择依据、核心关系式或目标函数和约束，
% 并解释模型各部分与实际对象的对应关系。下方预测模型与损失函数仅为公式排版示例，须按赛题改写。

\subsection{整体框架}
整体建模包括数据预处理、模型构建和模型评估三个主要阶段。

\subsection{核心模型}
\begin{equation}
\hat{y} = f(\mathbf{x}; \theta) = \sum_{i=1}^{n} \theta_i \phi_i(\mathbf{x})
\end{equation}

模型参数的优化目标是最小化正则化损失函数：
\begin{equation}
\mathcal{L}(\theta) = \frac{1}{N}\sum_{i=1}^{N} (y_i - \hat{y}_i)^2 + \lambda \|\theta\|_2^2
\end{equation}
